version: "3.3"
services:
  tomcat:
    image: tomcat:9.0-jdk17-corretto
    ports:
      - 8080:8080
    volumes:
      - ./war/:/usr/local/tomcat/webapps/
      - ./data/:/usr/local/tomcat/geoserver/data/
    command: /bin/sh -c "catalina.sh run"
    restart: always
volumes:
  war:
  data:
